# Sesión — 2026-07-17 — Martín

## Objetivo
Levantar backend y frontend en WSL2, diagnosticar bugs de runtime y dejarlos andando. Hacer pase responsive para que la app se vea bien en desktop (sacando la franja 4:3).

## Instrucciones
- Sin SDD formal (fixes y polish, no feature nueva).
- Tono Rioplatense (voseo) con Martín.
- No commitear `.env`.
- Commits convencionales, sin Co-Authored-By.
- Cualquier cambio cross-platform (Edgardo usa Windows puro sin WSL).

## Descubrimientos
- **.NET 9.0.306 no está en WSL2 Linux pero sí en Windows**. Se corre con `"/mnt/c/Program Files/dotnet/dotnet.exe"` desde WSL2.
- **WSL2 localhost ≠ Windows localhost**. El backend (Windows) bindea a `0.0.0.0:5193` y es accesible desde WSL2 vía IP del gateway (`172.26.64.1` en esta sesión — cambia al reiniciar WSL).
- **Supabase free tier está pausado/lentísimo**: 32 segundos por query, excediendo el command timeout de 30s de EF Core. Proyecto probablemente inactivo.
- **El admin user tiene `Name = null` en la DB** — rompía `GenerateJwtToken` porque `new Claim(ClaimTypes.Name, null)` tira `ArgumentNullException`.
- **Interceptor de axios redirigía a /login ante cualquier 401**, incluyendo `/auth/me` (que debe devolver 401 para no autenticados). Causaba reload infinito.
- **CSS `#root` tenía `width: 1126px` + `margin: 0 auto` + `border-inline` + `text-align: center`** — enmarcaba la SPA como panel centrado. En celular llenaba la pantalla, en desktop franja 4:3 con bordes.
- **`pnpm install` falló** por política de supply-chain (paquetes muy recién publicados). Se usó `npm install` como fallback.
- **`process.env.VITE_API_TARGET` no funciona en `vite.config.js`** — Vite no expone vars de `.env` en `process.env` para el config. Hay que usar `loadEnv()`.
- **Vite dev server se cae si el `nohup npm run dev` timeoutea** — los procesos hijo mueren con el timeout de bash.

## Completado
- ✅ Backend corriendo: `dotnet.exe run --urls "http://0.0.0.0:5193"` (proceso Windows)
- ✅ Frontend corriendo: `npm run dev -- --host 0.0.0.0` en puerto 5173 (proceso WSL2)
- ✅ Proxy Vite → backend funcionando vía `VITE_API_TARGET=http://172.26.64.1:5193`
- ✅ **Fix reload infinito** — `frontend/src/api/client.js`: interceptor excluye `/auth/me` y `/auth/logout` del redirect 401
- ✅ **Fix login roto** — `backend/Services/AuthService.cs`: `user.Name ?? string.Empty` en claims JWT y respuesta login
- ✅ **Fix franja 4:3** — `frontend/src/tokens.css`: `#root` sin width fijo, sin bordes, sin centrado global. 11 páginas + Footer unificadas a 3 tiers responsive: `max-w-7xl` (dashboards/listados), `max-w-5xl` (detalle/analytics), `max-w-3xl` (formularios)
- ✅ **Proxy configurable** — `frontend/vite.config.js`: usa `loadEnv('VITE_')` para `VITE_API_TARGET`. `frontend/.env.template`: documentada la nueva variable
- ✅ `package-lock.json` reinstaurado desde git (se borró en `npm install`)
- ✅ **Commit y push**: `452f7e0 fix: resolve dev environment startup issues and responsive layout` a `dev`
- ✅ Engram session summary guardado
- ✅ `.engram/sessions/2026-07-17-martin.md` y `2026-07-17-edgardo.md` creados

## Estado del proyecto
- **Backend**: corriendo en Windows, conectado a Supabase (lento). AuthService fix aplicado.
- **Frontend**: corriendo en WSL2. Layout responsive aplicado. Auth loop fix aplicado.
- **Base de datos**: Supabase free tier respondiendo con ~32s de latencia. Necesita reactivación en dashboard.
- **Tests**: no corridos en esta sesión (el objetivo era levantar y arreglar bugs de runtime).

## Próximos pasos
- Revisar visualmente 3 pantallas flageadas por posible pérdida de `text-align: center` heredado: StaffScan, TicketLookup, OrganizerEventNew
- Reactivar proyecto Supabase en dashboard (probablemente pausado por inactividad)
- Probar login con admin@ticketera.com
- Seedear datos de prueba para navegar la app completa
- Rotar credenciales de Supabase (están en git history)
- Correr tests del frontend (`npm test`) y backend (`dotnet test`)

## Archivos relevantes
- `backend/Services/AuthService.cs` — fix null Name en JWT (modificado)
- `frontend/src/api/client.js` — interceptor excluye auth endpoints (modificado)
- `frontend/src/tokens.css` — #root sin frame 4:3 (modificado)
- `frontend/vite.config.js` — loadEnv para VITE_API_TARGET (modificado)
- `frontend/.env.template` — documenta VITE_API_TARGET (modificado)
- `frontend/.env` — local, gitignoreado, contiene VITE_API_TARGET para WSL2 (nuevo, no commiteado)
- `frontend/src/pages/*.jsx` (11 archivos) — contenedores unificados 3 tiers (modificados)
- `frontend/src/components/layout/Footer.jsx` — ancho 7xl unificado (modificado)
